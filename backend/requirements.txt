fastapi==0.104.1
uvicorn==0.24.0
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
python-dotenv==1.0.0
pydantic==2.5.0
pydantic-settings==2.1.0
PyJWT==2.10.1
passlib==1.7.4
# passlib's bcrypt scheme needs the bcrypt package installed separately --
# it was missing entirely, so CryptContext(schemes=["bcrypt"]) raised
# MissingBackendError on the first register/login call. Pinned <4.1 because
# passlib 1.7.4 breaks against bcrypt>=4.1 (it probes bcrypt.__about__,
# which newer bcrypt releases removed).
bcrypt==4.0.1
python-multipart==0.0.6
email-validator==2.1.0
pdfplumber==0.10.3
python-docx==0.8.11
spacy==3.7.2
scikit-learn==1.3.2
numpy==1.26.2
pandas==2.1.3
aiofiles==23.2.1
httpx==0.27.0
# Verifies the ID token Google Identity Services hands back to the frontend
# on Sign in with Google -- confirms it's genuinely signed by Google for our
# GOOGLE_CLIENT_ID before we trust the email/name inside it.
google-auth==2.35.0
# Runs the external job-source sync on a recurring in-process schedule
# (see main.py) so listings refresh automatically instead of only when
# someone remembers to run data/sync_jobs.py by hand.
apscheduler==3.10.4
